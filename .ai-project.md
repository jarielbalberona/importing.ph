# AI Project Contract

Project: importing.ph

This project follows the workspace default:

Ticket-First, Docs-Grounded, Local-Verified Closed Loop.

## Source of truth

- `project-canon/` is the durable project source of truth.
- `docs/` may exist as legacy/reference material only.
- `.ai-project.md` defines local project agent rules.
- Parent `.ai/` defines shared workflow.
- `.ai-local/core-snapshot.md` is fallback only.
- Code and tests are executable truth.

## Project Index

The committed workspace project index template is:

- `.ai/workspace/project-index.example.md`

For local/private project routing, use:

- `.ai/workspace/project-index.local.md`

Prefer `project-index.local.md` when present. If it is absent, use `project-index.example.md` only as a format reference and infer the target project from the user's explicit path or instruction.

## Local Port Map

Use the workspace local port map before selecting local ports:

- `.ai/workspace/local-port-map.md`

Current importing.ph assignment:

- web and rendered SEO verification: `http://localhost:5001`
- PostgreSQL host port: `5032`
- PostgreSQL container port: `5432`
- mobile/Metro: not assigned; this repo is web-only

## Verification

Default: Local-Verified.

Appropriate verification is default. E2E is escalation, not default.

Use:

- `project-canon/operations/verification-profile.md`
- `project-canon/operations/agent-guardrails.md`

for project-specific verification and guardrail rules.

## Docs update rule

Update `project-canon/` only when durable project truth changes.
Do not update `project-canon/` for styling-only changes, one-off implementation details, temporary debugging notes, or noisy logs.
